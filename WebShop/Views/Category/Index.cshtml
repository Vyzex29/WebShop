@model List<CategoryModel>

@if(Model.Count == 0){
    <p>There are no categories</p>
}else
{
    <table class="table table-striped">
    <tr class="firstRow">
        <th>Id</th>
        <th>Name</th>
        <th>More Info</th>
    </tr>
    @foreach (var category in Model)
    {
        <tr>
            <td>@category.Id</td>
            <td>@category.Name</td>
            <td><a asp-area="" asp-controller="Category" asp-action="View" asp-route-id="@category.Id" asp-route-categoryName="@category.Name">More info</a></td>
        </tr>
    }
</table>
<button class="btn btn-secondary"><a asp-controller="Category" asp-action="Create">Add</a></button>

}